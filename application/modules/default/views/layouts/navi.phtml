<!-- navArea START --> 
<div id="navArea">

    <!-- navList START -->
    <div class="navList">

        <h3>カテゴリー</h3>
        <?php if ($this->categoryList !== false): //未分類も含めてひとつもカテゴリーが登録されていないか?>
        <ul>
            <?php foreach ($this->categoryList as $category): ?>
            <li>
                <?php if ($category['is_used']): //カテゴリーを使用しているページがあるか?>
                <a href='<?php echo $this->baseUrl("/page/category/id/{$category['id']}") ?>'><?php echo $category['name'] ?></a>
                <?php else: //使用していない場合はリンクしない?>
                <?php echo $category['name'] ?> 
                <?php endif; ?>
            </li>
            <?php endforeach; ?>
        </ul>
        <?php else: ?>
        <p>カテゴリーは登録されていません。</p>
        <?php endif; ?>
    </div>
    <!-- navList END -->

    <!-- navList START -->
    <div class="navList">
        <h3>タグクラウド</h3> 
                         
        <?php if ($this->tagCloud !== false): //タグがなかったら表示しない?>
        <ul id="tagCloud">
            <?php foreach ($this->tagCloud as $tag): ?>    
            <li class="level<?php echo $tag['value'] ?>">
                <a href="<?php echo $this->baseUrl("/page/tag/id/{$tag['id']}") ?>"><?php  echo $tag['title'] ?></a>
            </li> 
           <?php endforeach;?>
       </ul> 
       
       <?php else: ?>
       <p>タグは登録されていません</p>
       <?php endif;?>                     
    </div>
     <!-- navList END -->    
     
 </div>
<!-- navArea END --> 
