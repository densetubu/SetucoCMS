iss499ブランチめも

複数キーワード検索に対応後、AND ORの実装にとりかかりましたが、
途中でAND ORの実装を断念。
複数キーワードのみmasterにmergeして、リリース済み。
しかし、バグが残っている。
バグは別チケットにて対応する。
AND OR対応は保留とするが、途中ファイルを残すため、当ブランチを残留扱いにします。
当ブランチはmasterにmergeしないこと。参照目的のみで残しておきます。
AND ORの実装にとりかかる際には別チケットにて対応する。
必要がなくなった時点で削除する。